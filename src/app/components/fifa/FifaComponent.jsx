import React from "react";
import { bool, shape, string, func } from "prop-types";

const FifaComponent = props => {
  return (
    <div>
      <h2>Fifa</h2>
    </div>
  );
};

FifaComponent.defaultProps = {
};

FifaComponent.propTypes = {
};

export default FifaComponent;
